# Todoアプリ 仕様書

## 1. 概要

シンプルで軽量なTodoアプリ。タスクの追加・編集・削除・検索・フィルタリングを提供する。
データは Go API 経由でクラウド（PostgreSQL）に保存され、Google アカウントで認証してマルチデバイス間でリアルタイム同期する。

> アーキテクチャの詳細は [architecture.md](architecture.md)、API・DB・認証フローの詳細は [cloud-design.md](cloud-design.md) を参照。

---

## 2. 対応プラットフォーム

| プラットフォーム | 対応 |
|---|---|
| Android | ◎ |
| iOS | ◎ |
| Windows | ◎ |
| macOS | ◎ |

---

## 3. 技術スタック

### クライアント（Flutter）

| 項目 | 選定 | 備考 |
|---|---|---|
| フレームワーク | Flutter | 4プラットフォーム対応 |
| 言語 | Dart | |
| 状態管理 | Riverpod 3.x | `riverpod_generator` 併用 |
| HTTP クライアント | dio | OpenAPI スキーマを参照した手書き実装 |
| WebSocket | web_socket_channel | リアルタイム同期 |
| 認証 | Google OAuth（ブラウザ経由）+ JWT | `url_launcher` でブラウザ起動。`flutter_secure_storage` に JWT 保存 |

### サーバー（Go API）

| 項目 | 選定 | 備考 |
|---|---|---|
| 言語 | Go | |
| API スキーマ | TypeSpec → OpenAPI | `spec/main.tsp` が単一情報源 |
| HTTP サーバー | ogen 生成コード | `Handler` インターフェースを実装 |
| DB クライアント | sqlc + pgx/v5 | 型安全な SQL |
| マイグレーション | Atlas | `schema.sql` から差分自動生成 |
| 認証 | JWT (HS256) + Google ID Token 検証 | |

### データベース

PostgreSQL（`taskmanager` スキーマ）。すべての PK は UUID、ID は app 側で生成。

---

## 4. 機能要件

### 4.1 タスク管理

| 機能 | 内容 |
|---|---|
| タスク追加 | タイトル・メモ・期限日・優先度・タグ・ステータスを設定して作成 |
| タスク編集 | 各項目を後から変更可能（PUT で全置換） |
| タスク削除 | 個別削除（確認ダイアログあり） |

### 4.2 タスクの属性

| 属性 | 型 | 詳細 |
|---|---|---|
| タイトル | テキスト | 必須 |
| メモ | テキスト（任意） | |
| 期限日 | 日時（任意） | |
| 優先度 | 参照（任意） | 後述「4.3 優先度」参照 |
| タグ | 複数参照 | 後述「4.5 タグ」参照 |
| ステータス | 参照（必須） | 後述「4.4 ステータス」参照 |
| 作成日時 | 日時 | サーバー側で自動設定 |
| 更新日時 | 日時 | サーバー側で自動更新 |

### 4.3 優先度

ユーザー初回サインイン時にデフォルトデータが投入される。ユーザーが任意に追加・編集・削除可能。

| 優先度名（デフォルト） | 説明 |
|---|---|
| 絶対やる | 必ず対応が必要なタスク |
| 余裕があったらやる | 時間があれば対応するタスク |

### 4.4 ステータス

ユーザー初回サインイン時にデフォルトデータが投入される。ユーザーが任意に追加・編集・削除可能。

| ステータス名（デフォルト） |
|---|
| 未着手 |
| 進行中 |
| 完了 |

### 4.5 タグ

- 自由入力で作成（テキスト）
- 1つのタスクに複数のタグを付与可能
- 名前はユーザー内で一意
- タグを削除した場合、そのタグを持つタスクからも除去される

### 4.6 一覧表示

- タスクを一覧表示する
- デフォルトの表示順: 作成日時の降順（新しい順）
- タスクカードに表示する情報: タイトル、期限日、優先度、ステータス、タグ
- 「完了」ステータスのタスクはデフォルトで非表示（フィルタで表示切り替え可能）

### 4.7 フィルタリング

以下の条件で絞り込み可能。複数条件の組み合わせに対応。サーバー側でクエリパラメータとして処理。

| フィルタ項目 | 詳細 |
|---|---|
| タグ | 選択したタグを持つタスクのみ表示 |
| 優先度 | 選択した優先度のタスクのみ表示 |
| ステータス | 選択したステータスのタスクのみ表示（除外指定も可） |
| 期限日 | 期限切れフィルタ |
| 検索 | タイトルを対象にキーワード検索 |

### 4.8 ソート

以下の項目でソート可能（昇順・降順の切り替えあり）。

| ソート項目 |
|---|
| 作成日時（デフォルト）|
| 更新日時 |
| 期限日 |
| 優先度 |
| タイトル |

### 4.9 リアルタイム同期

- ログイン後、WebSocket（`/ws`）に接続
- 他端末でデータが変更されると `*.changed` イベントが配信される
- クライアントは該当 Provider を invalidate して自動再取得

---

## 5. データモデル

スキーマの詳細（CREATE TABLE 文・ER 図）は [cloud-design.md](cloud-design.md#3-postgresql-スキーマ) を参照。

### テーブル概要

| テーブル | 説明 |
|---|---|
| `users` | Google 認証ユーザー（`google_sub` でユニーク） |
| `priorities` | 優先度（ユーザーごと） |
| `statuses` | ステータス（ユーザーごと） |
| `tags` | タグ（ユーザーごと・名前はユーザー内でユニーク） |
| `tasks` | タスク（ユーザーごと） |
| `task_tags` | タスク↔タグの多対多中間テーブル |

すべての PK は UUID。`user_id` FK により各ユーザーのデータが分離される（`ON DELETE CASCADE`）。

---

## 6. 画面構成

```
未ログイン:
┌─────────────────────────────────┐
│ ログイン画面                     │
│  - Google サインインボタン       │
└─────────────────────────────────┘
         ↓ サインイン成功
┌─────────────────────────────────┐
│ タスク一覧画面（メイン）         │
│  - 検索バー                      │
│  - フィルタ・ソートバー          │
│  - タスクカード一覧              │
│  - タスク追加ボタン（FAB）       │
└─────────────────────────────────┘
         ↓ タスクをタップ
┌─────────────────────────────────┐
│ タスク詳細・編集画面             │
│  - 各属性の表示・編集            │
│  - 削除ボタン                    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 設定画面                         │
│  - 優先度の管理（追加・編集・削除）│
│  - ステータスの管理               │
│  - タグの管理                    │
│  - サインアウト                   │
└─────────────────────────────────┘
```

---

## 7. 非機能要件

| 項目 | 内容 |
|---|---|
| ネットワーク | オンライン専用（API 通信前提） |
| 認証 | Google OAuth 必須 |
| UI言語 | 日本語 |
| データ保存 | クラウド（PostgreSQL） |
| マルチデバイス | 同一 Google アカウントで複数端末から同時利用可能 |
